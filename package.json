{
  "name": "synctag-devextension-qa",
  "version": "1.0.0",
  "description": "Playwright QA automation suite for devextension.synctag.com (signup/login, pricing, Razorpay checkout)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:auth": "playwright test tests/auth.spec.ts",
    "test:pricing": "playwright test tests/pricing.spec.ts",
    "test:checkout": "playwright test tests/checkout.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "typescript": "^5.9.3"
  }
}
